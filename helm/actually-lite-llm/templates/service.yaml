apiVersion: v1
kind: Service
metadata:
  name: {{ include "actually-lite-llm.fullname" . }}
  labels:
    {{- include "actually-lite-llm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "actually-lite-llm.selectorLabels" . | nindent 4 }}
